baseURL = 'https://example.org/'
languageCode = 'en'
title = 'My New Hugo Site'
theme = 'hugo-theme-reimu'

newContentEditor = "content/posts"  # 使用直接赋值格式

# ===== 修正后的配置 =====
paginate = 10  # 直接指定分页数量（整数）

[params]
  # 指定在首页显示的内容类型
  mainSections = ["posts"]  # 必须与 newContentEditor 的目录名一致

[frontmatter]
  defaultContentType = "posts"  # 与 mainSections 匹配
# =====================

defaultContentLanguage = 'zh-CN'
[languages]
[languages.en]
languageName = 'English'
weight = 1
[languages.zh-CN]
languageName = '简体中文'
weight = 1
hasCJKLanguage = true
[languages.zh-TW]
languageName = '繁體中文'
weight = 1
hasCJKLanguage = true
[languages.ja]
languageName = '日本語'
weight = 1
hasCJKLanguage = true

[markup.goldmark.extensions.passthrough]
enable = true
delimiters.block = [["\\[", "\\]"], ["$$", "$$"]]
delimiters.inline = [["\\(", "\\)"], ["$", "$"]]

[markup.highlight]
guessSyntax = true
noClasses = false

[outputs]
home = ["Algolia", "HTML", "RSS"]

[outputFormats.Algolia]
baseName = "algolia"
isPlainText = true
mediaType = "application/json"
notAlternative = true